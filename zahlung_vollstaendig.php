<?php
declare(strict_types=1);
require_once __DIR__."/db.php";require_once __DIR__."/funktionen.php";require_once __DIR__."/zahlungs_service.php";
$ajax=strtolower((string)($_SERVER["HTTP_X_REQUESTED_WITH"]??""))==="xmlhttprequest";if($ajax)header("Content-Type: application/json; charset=UTF-8");
try{if($_SERVER["REQUEST_METHOD"]!=="POST")throw new EingabeException("Ungültige Anfragemethode.");csrf_pruefen($_POST["csrf_token"]??null);berechtigung_pruefen(["administrator","mitarbeiter"]);$auftragId=positive_id($_POST["auftrag_id"]??null,"Auftrag");$zuordnungId=positive_id($_POST["zuordnung_id"]??null,"Gerät");$daten=geraet_vollstaendig_bezahlen($pdo,$auftragId,$zuordnungId,(string)($_POST["zahlungsdatum"]??""),(string)($_POST["zahlungsart"]??""),$_POST["notiz"]??null);$antwort=["ok"=>true,"auftrag_id"=>$auftragId,"zuordnung_id"=>$zuordnungId,"nachricht"=>"Die vollständige Zahlung wurde gespeichert."]+$daten;if($ajax){echo json_encode($antwort,JSON_UNESCAPED_UNICODE);exit;}flash_setzen("success",$antwort["nachricht"]);weiterleiten("auftraege.php");}
catch(Throwable $e){$meldung=$e instanceof EingabeException?$e->getMessage():"Die Zahlung konnte nicht gespeichert werden.";if($ajax){http_response_code($e instanceof EingabeException?422:500);echo json_encode(["ok"=>false,"fehler"=>$meldung],JSON_UNESCAPED_UNICODE);exit;}flash_setzen("error",$meldung);weiterleiten("auftraege.php");}
